document.addEventListener('DOMContentLoaded', function() {
    const marqueeInner = document.querySelector('.animation-row__inner' );
    const text = marqueeInner.innerHTML;
    marqueeInner.innerHTML += text;
});

document.addEventListener('DOMContentLoaded', function() {
    const marqueeInner = document.querySelector( '.animation-row__inner_orange');
    const text = marqueeInner.innerHTML;
    marqueeInner.innerHTML += text;
});


